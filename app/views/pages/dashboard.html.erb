<div class="container">
  <div class="row">
    <div class="col-12 col-sm-6 my-3">
      <div class="booking-cards">


        <h1>My dogs </h1>
          <% if @dogs.empty? %>
             <p>You don't have any dog for the moment</p>
            <%= link_to "Add a new truffle", new_dog_path, class:"button-first" %>
            <% else %>
              <div class="booking-card">
                <% @dogs.each do |dog| %>
                  <%= dog.name %>
                  <%=  %>
                  <%= link_to "Add a new truffle", new_dog_path, class:"button-first"  %>
                <% end %>
              </div>
          <% end %>

<h1>My pending booking requests</h1>
 <% @bookings_pending.each_with_index do |booking, index| %>
          <% dog = Dog.find(booking.dog_id) %>
          <% user = User.find(dog.user_id) %>
          <% review = Review.new %>
          <div class="booking-card">
            <% if dog.photos.attached? %>
              <%= cl_image_tag dog.photos[0].key, class:"booking-card-image" %>
            <% else %>
              <%= image_tag 'home.jpg', class:"booking-card-image" %>
            <% end %>
            <div class="booking-card-content">
              <div class="booking-card-title">
                <h4><%= link_to dog.name, dog_path(dog) %></h4>
                <% if booking.reviews.length != 0 %>
                  <button type="button" class="button-second" data-toggle="modal" data-target="#review-modal<%=index%>">
                    See your review
                  </button>
                <% elsif booking.status == "approved" %>
                  <button type="button" class="button-second" data-toggle="modal" data-target="#create-review-modal<%=index%>">
                    Create a review
                  </button>
                <% end %>
              </div>
              <div class="booking-card-dates">
                <div>
                  <p>from <strong><%= booking.start_date %></strong></p>
                  <p>to <strong><%= booking.end_date %></strong></p>
                </div>
              </div>
            </div>
          </div>
<% end %>




<h1>My booking history</h1>

 <% @bookings_history.each_with_index do |booking, index| %>
          <% dog = Dog.find(booking.dog_id) %>
          <% user = User.find(dog.user_id) %>
          <% review = Review.new %>
          <div class="booking-card">
            <% if dog.photos.attached? %>
              <%= cl_image_tag dog.photos[0].key, class:"booking-card-image" %>
            <% else %>
              <%= image_tag 'home.jpg', class:"booking-card-image" %>
            <% end %>
            <div class="booking-card-content">
              <div class="booking-card-title">
                <h4><%= link_to dog.name, dog_path(dog) %></h4>
                <% if booking.reviews.length != 0 %>
                  <button type="button" class="button-second" data-toggle="modal" data-target="#review-modal<%=index%>">
                    See your review
                  </button>
                <% elsif booking.status == "approved" %>
                  <button type="button" class="button-second" data-toggle="modal" data-target="#create-review-modal<%=index%>">
                    Create a review
                  </button>
                <% end %>
              </div>
              <div class="booking-card-dates">
                <div>
                  <p>from <strong><%= booking.start_date %></strong></p>
                  <p>to <strong><%= booking.end_date %></strong></p>
                </div>
              </div>
            </div>
          </div>
<% end %>



      </div>
    </div>
  </div>
</div>
